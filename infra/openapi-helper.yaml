openapi: 3.0.1
info:
  title: OpenAI Connector Helper API
  description: This is an API for OpenAI Connector Helper.
  version: 1.0.0
servers:
  - url: http://localhost:7071/api
paths:
  /convert/audio:
    post:
      tags:
        - converter
      operationId: ConvertAudioFormat
      requestBody:
        description: The input file data
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/audioFormatRequestModel'
      responses:
        '200':
          description: The output file data
          content:
            audio/wav:
              schema:
                type: string
                format: binary
        '400':
          description: Either request header or body is invalid
          content:
            text/plain:
              schema:
                type: string
        '404':
          description: Resource not found
          content:
            text/plain:
              schema:
                type: string
        '500':
          description: Something went wrong
          content:
            text/plain:
              schema:
                type: string
      security:
        - function_key: [ ]
  '/convert/stt/{locale}':
    post:
      tags:
        - converter
      operationId: ConvertSpeechToText
      parameters:
        - name: locale
          in: path
          description: The locale of the input file
          required: true
          schema:
            type: string
      requestBody:
        description: The input file data
        content:
          audio/wav:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: The output file data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/speechToTextResponseModel'
        '400':
          description: Either request header or body is invalid
          content:
            text/plain:
              schema:
                type: string
        '404':
          description: Resource not found
          content:
            text/plain:
              schema:
                type: string
        '500':
          description: Something went wrong
          content:
            text/plain:
              schema:
                type: string
      security:
        - function_key: [ ]
components:
  schemas:
    audioFormatRequestModel:
      type: object
      properties:
        input:
          type: string
        output:
          type: string
        inputData:
          type: string
    speechToTextResponseModel:
      type: object
      properties:
        outputData:
          type: string
  securitySchemes:
    function_key:
      type: apiKey
      name: x-functions-key
      in: header